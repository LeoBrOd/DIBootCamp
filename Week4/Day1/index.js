// Exercise 1\
function age(myAge, mumAge, dadAge) {
let myAge = prompt("How are old are you?")
let mumAge = myAge * 2
let dadAge = mumAge * 1.2
alert("My mum is " + mumAge + " my dad is " + dadAge)
console.log (mumAge)}
age()
